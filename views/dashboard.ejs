<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="min-h-screen flex items-center justify-center">
        <script>
            alert("Login Successfully");
        </script>

        <!-- Main Dashboard -->
        <div class="bg-zinc-800 p-8 rounded-lg shadow-lg w-full max-w-lg">
            <h1 class="text-3xl font-bold mb-6 text-center">Welcome, <%= user.name %>!</h1>

            <!-- User Information -->
            <div class="space-y-4">
                <div class="flex justify-between">
                    <span class="text-lg font-medium">Name:</span>
                    <span class="text-lg"><%= user.name %></span>
                </div>

                <div class="flex justify-between">
                    <span class="text-lg font-medium">Email:</span>
                    <span class="text-lg"><%= user.email %></span>
                </div>

                <div class="flex justify-between">
                    <span class="text-lg font-medium">Age:</span>
                    <span class="text-lg"><%= user.age %></span>
                </div>
            </div>

            <!-- Logout Button -->
            <div class="mt-6 text-center">
                <form action="/logout" method="POST">
                    <button type="submit" class="bg-red-500 hover:bg-red-600 text-white py-2 px-4 rounded-lg w-full">
                        Logout
                    </button>
                </form>
            </div>
            <div class="mt-6 text-center">
                <form action="/delete" method="POST">
                    <button type="submit" class="bg-red-500 hover:bg-red-600 text-white py-2 px-4 rounded-lg w-full">
                        Delete Account
                    </button>
                </form>
            </div>
        </div>

    </div>
</body>
</html>
