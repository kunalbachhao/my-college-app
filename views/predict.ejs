<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>College Predictor</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 min-h-screen flex flex-col items-center justify-start py-12">

  <h2 class="text-2xl font-semibold text-white text-center mt-6">College Predictor</h2>

  <form action="/predict" method="POST" class="bg-zinc-800 p-8 rounded-lg w-96 shadow-lg mt-8">
    <label for="rank" class="block text-white">Enter your rank:</label>
    <input
      type="number"
      name="rank"
      id="rank"
      required
      placeholder="e.g. 12345"
      class="w-full p-3 mt-1 rounded-lg bg-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-600 mb-6"
    />
    
    <button
      type="submit"
      class="w-full py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-600"
    >
      Predict Colleges
    </button>
  </form>

  <% if (colleges && colleges.length > 0) { %>
    <div class="mt-8 text-white text-center max-w-md w-full px-4">
      <h3 class="text-xl mb-4">Predicted Colleges:</h3>
      <ul>
        <% colleges.forEach(college => { %>
          <li class="mb-2">
            <strong><%= college.name %></strong> - <%= college.branch %> (<%= college.location %>)
          </li>
        <% }) %>
      </ul>
    </div>
  <% } else if (colleges) { %>
    <p class="text-center text-white mt-8 max-w-md w-full px-4">No colleges found for this rank.</p>
  <% } %>

</body>
</html>